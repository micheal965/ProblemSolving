namespace Problem_Solving
{
    class IsPalindrome
    {
        //    Console.WriteLine(IsPalindrome("A man, a plan, a canal: Panama"));

        //    public static bool IsPalindrome(string s)
        //    {
        //        string AlphaNumStr = new string(s.Where(c => char.IsLetterOrDigit(c)).ToArray());
        //        var ReversedStr = new StringBuilder();
        //        for (int i = AlphaNumStr.Length - 1; i >= 0; i--)
        //            ReversedStr.Append(AlphaNumStr[i]);

        //        return string.Compare(AlphaNumStr, ReversedStr.ToString(), StringComparison.OrdinalIgnoreCase) == 0;
        //    }
    }
}
