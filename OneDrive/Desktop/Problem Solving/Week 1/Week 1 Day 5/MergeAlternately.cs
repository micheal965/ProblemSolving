namespace Problem_Solving.Week_1_Day_5
{
    class MergeAlternately
    {
        //MergeAlternately("ab", "pqrs");

        //public static string MergeAlternately(string word1, string word2)
        //{
        //    var Result = new StringBuilder();
        //    int Length = Math.Min(word2.Length, word1.Length);
        //    string word = word1.Length > word2.Length ? word1 : word2;

        //    for (int i = 0; i < Length; i++)
        //        Result.Append(word1[i]).Append(word2[i]);

        //    for (sbyte i = (sbyte)Length; i < word.Length; i++)
        //        Result.Append(word[i]);

        //    return Result.ToString();
        //}
    }
}
